TO DO LIST:

resoudre pb erase avec iterator. Erase detruit l'iterator donc on ne peut plus avoir le next;
faire value_compare => creer une structure a l'interieur de la classe ?
tester const lower et upper bound quand j'aurais refait const_iterator;
